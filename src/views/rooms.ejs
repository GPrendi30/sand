<% let my_rooms = [{"title": "Room1" , "img" : "/images/chat.png" }, {"title": "Room2" , "img" : "/images/chat.png" }, {"title": "Room3" , "img" : "/images/chat.png" }] %>
<% let other_rooms = [{"title": "Chat 1" , "img" : "/images/chat.png" }, {"title": "Chat 2" , "img" : "/images/chat.png" }, {"title": "Chat 3" , "img" : "/images/chat.png" }] %>

<section id="rooms">

    <!-- pubblic rooms -->
    <article class="your_rooms" id="general">
      <h1>Rooms You've Joined</h1>
      <ul class="discover_lineup">
            <!-- Used place holders -->
            
            <% rooms_.forEach( item => { %>
                <li class="discover_item grow_hover shadow"><a href="/item/info">
                    <img src="<%= item.img %>" alt="<%= item.title %>">
                    <h1><%= item.title %></h1>
                </a></li>
            <% }) %>
  
      </ul>
    </article>

    <!-- private rooms -->
    <article class="explore_rooms" id="general">
        <h1>Explore Rooms</h1>
        <ul class="discover_lineup">
              <!-- Used place holders -->
              <% rooms.forEach( item => { %>
                  <li class="discover_item grow_hover shadow"><a href="/item/info">
                      <img src="<%= item.img %>" alt="<%= item.title %>">
                      <h1><%= item.title %></h1>
                  </a></li>
              <% }) %>
    
        </ul>
      </article>
    
    <!-- too see the invites to join privates rooms -->
    <section class="invites">
        <%= room.invites %>
        <button id="accept_invite">accept invite</button>
    <section>
  
  </section>